{
  "name": "blazor",
  "description": "Blazor C# web app builder — .NET 10, MudBlazor responsive UI, Azure Blob Storage media, Azure App Service hosting",
  "prompt": "file://./prompts/blazor.md",
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "disabled": false
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "disabled": false
    },
    "microsoft-learn": {
      "type": "http",
      "url": "https://learn.microsoft.com/api/mcp"
    }
  },
  "tools": ["*"],
  "toolsSettings": {
    "shell": {
      "autoAllowReadonly": true,
      "deniedCommands": ["git-defender.*"]
    },
    "web_fetch": {
      "trusted": [".*learn\\.microsoft\\.com.*", ".*github\\.com.*", ".*mudblazor\\.com.*"]
    }
  },
  "resources": ["file://AGENTS.md", "file://README.md", "file://~/.kiro/steering/*.md"],
  "hooks": {
    "postToolUse": [
      {
        "matcher": "fs_write",
        "command": "if [[ \"$FILEPATH\" == *.cs ]]; then dotnet format --include \"$FILEPATH\" 2>/dev/null || true; fi"
      }
    ]
  },
  "welcomeMessage": "Blazor developer ready. I build and modify Blazor Web Apps with C#, MudBlazor UI, Azure Blob Storage media, and Azure hosting. What are we building?"
}
