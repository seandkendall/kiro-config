{
  "name": "testing",
  "description": "QA and testing agent — Cypress E2E via subagent, pytest, Jest/Vitest, targeting 100% coverage",
  "prompt": "You are an expert QA and testing agent. Target 100% coverage, never below 90%.\n\nE2E: ALWAYS delegate Cypress E2E work to the 'cypress' subagent via use_subagent. Don't write Cypress tests yourself.\n\nTEST FRAMEWORKS: pytest + moto for Python/Lambda, Vitest + React Testing Library for React, npm/yarn for runners.\n\nSIDE CHANNELS (Kiro CLI 2.3.0+): When running long test suites in shell scripts, route verbose output to `$AGENT_DISPLAY_OUT` (visible in TUI but kept out of agent context) and structured pass/fail summaries to `$AGENT_CONTEXT_OUT` (captured as agent_notes). Example: `npx vitest --coverage 2>&1 | tee \"$AGENT_DISPLAY_OUT\"; echo \"Coverage: $PCT%\" > \"$AGENT_CONTEXT_OUT\"`. See skills/deploy.sh.template for the full pattern.\n\nMCP PREFERENCE: Use `@chrome-devtools` for browser debugging instead of curl/wget. Use `@github` for any GitHub API operations.\n\nCONTEXT TIPS: Use @path syntax to reference files inline — saves tool calls and tokens.",
  "mcpServers": {
    "browsermcp": {
      "command": "npx",
      "args": ["@browsermcp/mcp@latest"],
      "disabled": false
    },
    "chrome-devtools": {
      "command": "npx",
      "args": [
        "-y",
        "chrome-devtools-mcp@latest",
        "--channel=canary",
        "--autoConnect",
        "--headless=true"
      ],
      "disabled": false
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "disabled": false
    },
    "aws-mcp-server": {
      "command": "uvx",
      "args": [
        "mcp-proxy-for-aws@latest",
        "https://aws-mcp.us-east-1.api.aws/mcp",
        "--metadata",
        "AWS_REGION=us-east-1"
      ],
      "disabled": false
    }
  },
  "tools": ["*"],
  "resources": [
    "file://AGENTS.md",
    "file://README.md",
    "file://~/.kiro/steering/*.md",
    "skill://~/.kiro/skills/testing-patterns.md"
  ],
  "toolsSettings": {
    "shell": {
      "timeout": 300,
      "working_directory": ".",
      "autoAllowReadonly": true,
      "deniedCommands": ["git-defender.*"]
    },
    "web_fetch": {
      "trusted": [".*docs\\.aws\\.amazon\\.com.*"]
    }
  },
  "keyboardShortcut": "ctrl+6",
  "welcomeMessage": "QA specialist ready. pytest + moto for Lambda, Vitest + RTL for React, Cypress E2E via subagent. Target: 100% coverage. What needs testing?",
  "hooks": {
    "postToolUse": [
      {
        "matcher": "fs_write",
        "command": "ruff check --fix --quiet \"$FILEPATH\" 2>/dev/null; ruff format --quiet \"$FILEPATH\" 2>/dev/null || true"
      },
      {
        "matcher": "fs_write",
        "command": "npx prettier --write \"$FILEPATH\" 2>/dev/null || true"
      }
    ]
  }
}
